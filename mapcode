#include <bits/stdc++.h>
using namespace std;

int main()
{
   string p = "forxxorfxdofr";
   
   unordered_map<char, int> mp;
   
   for(auto i: p){
       mp[i]++;
   }
   
    for(auto x: mp){
        cout<<x.first<<" "<<x.second<<endl;
    }
    return 0;
}
